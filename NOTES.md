<!-- This is the static design preview — the full-design HTML demo for standalone browser preview without any build step -->
<!-- The React Vite app is in src/ and can be run with: npm install && npm run dev -->

See index.html for the React app entry point.
To preview the design, open DESIGN_PREVIEW.html in a browser.
